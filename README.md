# Test Deployment Workflow

This repo was developed to test a Github Actions workflow that included publishing to Docker Hub, and then deploying the change automatically to a web server via Docker Compose.

